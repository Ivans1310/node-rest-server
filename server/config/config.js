//=====================================
// PORT
//=====================================
// if the env is on production the port is asigned by the hosting else the port 3000 was by default
PORT = process.env.PORT || 3000;

module.exports = {
    PORT
}